{
    "httpPort":8080,
    "socketPort":8081,
    "socketUrl":"ws://localhost:8081",
    
    /* The maximum number of failed auth attempts. */
    "authFailLimit":12,
    
    /* The interval in milliseconds between user account unlock events. */
    "accountUnlockerInterval":1800000, // 30 * 60 * 1000 which is 30 minutes.
}